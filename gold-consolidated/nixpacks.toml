[phases.setup]
nixPkgs = ["python311Full", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn gold_server:app --host 0.0.0.0 --port ${PORT:-8001}"
